{
  "name": "devnotes-oauth-provider",
  "version": "1.0.0",
  "description": "OAuth provider for DevNotes Scribe CMS",
  "main": "api/auth.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}

